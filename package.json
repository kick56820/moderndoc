{
  "name": "pbdocs-node",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone PowerBuilder 10.5 Help browser.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node scripts/check-parser-samples.js && node scripts/check-parser-warnings.js",
    "release:check": "node scripts/release-check.js",
    "release:package": "node scripts/package-release.js"
  },
  "engines": {
    "node": ">=12"
  }
}
